"""Hosts the neural networks as they might get way larger with many customizations

Examples could be a neural network for a classifier or a neural network for a regressor. Or
a MLP or Multi-Headed MLP.
"""

# helpers
import logging


# set logger
logger = logging.getLogger(__name__)
